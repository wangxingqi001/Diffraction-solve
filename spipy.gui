# !/bin/bash
if [ -z $1 ];then
	SCRIPTPATH="$( cd "$(dirname "$0")" ; pwd -P )"
	/usr/bin/env python -W ignore $SCRIPTPATH/src/start_app.py
else
	echo "**************************************"
	echo "GUI of spipy program, VERSION 1.0"
	echo "	GNU LICENCE v3.0 "
	echo "	Compatible with spipy version 2.x "
	echo "Contributor :"
	echo "	shiyingchen, CSRC"
	echo "	             Tsinghua Univ."
	echo "**************************************"
fi
